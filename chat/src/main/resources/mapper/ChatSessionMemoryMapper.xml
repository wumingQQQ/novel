<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wuming.chat.infrastructure.mapper.ChatSessionMemoryMapper">

    <!-- 使用乐观版本号写入摘要，避免并发请求覆盖较新的长期记忆。 -->
    <update id="updateIfVersionMatches">
        UPDATE chat_session_memories
        SET summary_content = #{memory.summaryContent},
            covered_message_id = #{memory.coveredMessageId},
            version = #{memory.version},
            update_time = CURRENT_TIMESTAMP
        WHERE session_id = #{memory.sessionId}
          AND version = #{expectedVersion}
    </update>
</mapper>
